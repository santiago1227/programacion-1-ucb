function mi_primer_algoritmo(){
    var mensaje = "hola mundo";
    alert (mensaje);
}

// return

function binarioADecimal(binario) {
  let decimal = 0;
  for (let i = 0; i < binario.length; i++) {
    digitobinario = binario[1];
    if(digitobinario === "1"){
        decimal = decimal * 2 + 1;
  }
else{

      decimal = decimal * 2 + 0

    }  
    
    alert(decimal);

  }
  function sumaprimos(){

}    



  
  }
  return decimal;


